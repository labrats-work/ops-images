name: 'omnibus'

on:
  push:
    # branches:
    #   - "main"
  workflow_dispatch:

jobs:
  build-and-test:
    uses: ./.github/workflows/build.yml
    with:
      context: ./src/omnibus
      registry_image: ghcr.io/${{ github.repository }}/omnibus
    secrets: inherit